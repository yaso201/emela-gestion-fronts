# ============================================================
# emela SIRH — back-office RH/MSS — variables d'environnement
# Copier en `.env` (non versionné) et renseigner.
# Seules les variables préfixées PUBLIC_ sont exposées au client.
# ============================================================

# URL de base du back Frappe/benin_hr (cf. self-service).
#  - VIDE (défaut) = même origine (reverse-proxy sous le domaine Frappe).
#  - SINON, URL absolue, ex. https://hr.lanem.bj (CORS + cookies, cf. DEPLOYMENT.md).
PUBLIC_FRAPPE_URL=
